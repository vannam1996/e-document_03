<%= form_for @document.favorites.build, remote: true do |f| %>
  <div><%= hidden_field_tag :document_id, @document.id %></div>
  <%= f.submit t("favorite.btn_add"), class: "btn btn-success" %>
<% end %>
