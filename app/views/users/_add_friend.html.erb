<%= form_for current_user.active_friends.build, remote: true do |f| %>
  <div><%= hidden_field_tag :accepter_id, @user.id %></div>
  <%= f.submit t(".send_request"), class: "btn btn-primary" %>
<% end %>
